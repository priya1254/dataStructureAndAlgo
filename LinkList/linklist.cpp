#include <bits/stdc++.h>
#include<sstream>
using namespace std;



mergeTwoList(list l1,list l2)
{

   list l3;
     sort()
    for (list<int>::iterator it=l1.begin(),list<int>::iterator it2=l2.begin(),; it != l1.end()&& it2!= l2.end();  ++it, ++it2)
    if(*it)
}
int main()
{


mergeTwoList(l1, l2);

return(0);
}
